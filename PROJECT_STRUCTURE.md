# 프로젝트 구조

포트폴리오 프로젝트의 디렉토리 구조 및 파일 용도 설명

## 📁 디렉토리 구조

```
Portfolio_project/
├── app/                    # Next.js App Router (프론트엔드 + 백엔드)
│   ├── api/               # API 라우트 (백엔드)
│   │   ├── admin/        # 관리자 API
│   │   ├── auth/         # 인증 API
│   │   ├── chat/         # 챗봇 API
│   │   ├── dify/         # Dify API 프록시
│   │   └── mcp/          # MCP 연동 API
│   ├── admin/            # 관리자 페이지
│   ├── contact/          # 연락처 페이지
│   ├── messages/         # 메시지 게시판 페이지
│   ├── profile/          # 프로필 페이지
│   ├── projects/         # 프로젝트 페이지
│   ├── publications/     # 논문 페이지
│   ├── layout.tsx        # 루트 레이아웃
│   └── page.tsx          # 홈 페이지
│
├── components/            # React 컴포넌트 (프론트엔드)
│   ├── HomeChatbot.tsx   # 홈 챗봇 컴포넌트
│   ├── Navigation.tsx    # 네비게이션 컴포넌트
│   ├── PaperAI.tsx       # 논문 AI 분석 컴포넌트
│   └── PaperChat.tsx     # 논문 채팅 컴포넌트
│
├── lib/                   # 유틸리티 라이브러리
│   ├── ai/               # AI 관련 유틸리티
│   │   ├── dify.ts       # Dify API 클라이언트
│   │   └── google-ai.ts  # Google AI 클라이언트
│   ├── pdf/              # PDF 처리
│   │   └── parser.ts     # PDF 파서
│   ├── rag/              # RAG (Retrieval-Augmented Generation)
│   │   └── search.ts     # 벡터 검색
│   └── supabase/         # Supabase 클라이언트
│       ├── admin.ts      # 관리자 클라이언트
│       ├── client.ts     # 클라이언트 사이드 클라이언트
│       └── server.ts     # 서버 사이드 클라이언트
│
├── database/              # 데이터베이스 스크립트
│   └── supabase/         # Supabase SQL 파일
│       ├── schema.sql
│       ├── functions.sql
│       └── ...
│
├── docs/                  # 문서
│   ├── setup/            # 설정 가이드
│   ├── guides/           # 사용 가이드
│   ├── troubleshooting/  # 문제 해결
│   ├── data/             # 데이터 가이드
│   ├── README.md         # 프로젝트 개요
│   ├── PRD.md            # 제품 요구사항
│   └── DEPLOY.md         # 배포 가이드
│
├── scripts/               # 스크립트 파일
│   ├── upload-documents.ts
│   └── ...
│
├── public/                # 정적 파일
│   ├── paper.pdf         # 논문 PDF
│   └── profile-photo.jpeg
│
├── 문서/                  # 원본 문서 (참고용)
│   ├── profile.md
│   └── ...
│
├── middleware.ts          # Next.js 미들웨어
├── next.config.ts        # Next.js 설정
├── package.json          # 프로젝트 의존성
└── tsconfig.json         # TypeScript 설정
```

## 📂 주요 디렉토리 설명

### `/app` - Next.js App Router
- **프론트엔드**: 페이지 컴포넌트 (`page.tsx`)
- **백엔드**: API 라우트 (`api/`)

### `/components` - React 컴포넌트
재사용 가능한 UI 컴포넌트

### `/lib` - 유틸리티 라이브러리
- AI 통합 (Dify, Google AI)
- PDF 처리
- RAG 검색
- Supabase 클라이언트

### `/database` - 데이터베이스
SQL 스크립트 및 마이그레이션 파일

### `/docs` - 문서
프로젝트 문서 및 가이드

### `/scripts` - 스크립트
자동화 스크립트 및 유틸리티

### `/public` - 정적 파일
이미지, PDF 등 공개 파일

## 🔄 파일 이동 이력

프로젝트 정리를 위해 다음 파일들이 이동되었습니다:

- **문서 파일** → `docs/` 하위 디렉토리로 분류
- **SQL 파일** → `database/supabase/`로 이동
- **원본 문서** → `문서/` 디렉토리 유지

## 📝 참고사항

- Next.js App Router 구조를 따르므로 `app/` 디렉토리 구조는 변경하지 않습니다
- 컴포넌트는 `components/` 디렉토리에 유지합니다
- 모든 import 경로는 자동으로 업데이트됩니다 (TypeScript/Next.js)

